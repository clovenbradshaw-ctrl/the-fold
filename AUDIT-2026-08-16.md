# Fold audit — verified punch list (2026-08-16)

Read-only audit, 5 lenses, every top finding adversarially verified against the bytes (10 confirmed, 0 refuted, 25 reported unverified below).

## Confirmed

### 1. [high] quotes.js:298

A quotation containing a fabricated ellipsis segment (status "partial") is never flagged by quoteFindings/quoteOpens, and applyQuotes appends an inline chunk address to it — a mechanical warrant stamped on invented words, the exact false-warrant class the module exists to refuse.

**Evidence:** Status assignment (quotes.js:223-227): `if (!located.length) status = "unlocated"; else if (inPoolOnly) status = "outside-offer"; else if (missing) status = "partial";`. quoteFindings (line 311) filters `.filter((q) => q.status === "unlocated")` and quoteOpens (line 319) filters `"outside-offer"` — "partial" reaches neither, and holon.js:570 folds only `quoteFindings(quotes)` into `unsupported`, so the fabricated segment never drives correction. Meanwhile applyQuotes line 298: `if (refs.length && !q.alreadyCited && q.status !== "outside-offer") { tail = " " + refs.map((r) => `[${r}]`).join(" "); cited.push(...refs); }` — "partial" passes this gate. Verified against quotes.test.mjs's own corpus: answer `The minutes say "the harbour committee had disputed the silting figure ... and agreed to bury the report before the equinox" that night.` (second segment nowhere in the material) → status "partial", unlocatedSegments 1, quoteFindings [], quoteOpens [], and applyQuotes returns `...before the equinox" [assembly.txt#0-187] that night.` with cited: ["assembly.txt#0-187"]. The half-invented quotation ships silently cited.

**Verification:** Confirmed against the bytes and reproduced empirically. quotes.js:222-227 assigns status "partial" when a span has at least one located segment and at least one missing one; quoteFindings (line 311) filters only "unlocated" and quoteOpens (line 319) only "outside-offer", so "partial" reaches neither; holon.js:570 folds only quoteFindings into unsupported (the correction driver) and holon.js:839 adds only quoteOpens — grep shows no consumer anywhere reads "partial" or unlocatedSegments. The citation gate at quotes.js:298 (`refs.length && !q.alreadyCited && q.status !== "outside-offer"`) passes "partial" because the located segment supplies a ref. Ran the exact scenario with node against the real modules using quotes.test.mjs's own corpus: answer quoting "the harbour committee had disputed the silting figure ... and agreed to bury the report before the equinox" (second segment absent from the material) produced status "partial", unlocatedSegments 1, quoteFindings [], quoteOpens [], and applyQuotes output `...before the equinox" [assembly.txt#0-187] that night.` with cited ["assembly.txt#0-187"] — an inline chunk address stamped on a quote whose second half is invented. No guard exists elsewhere: quotes.test.mjs has zero partial-status tests, grounding.js checks only figures/names (the fabricated segment is lowercase common nouns — the exact blind spot quotes.js's header says it exists to close), the relation tier is optionally injected (holon.js:534 `makeRelationReader?.() ?? null`), and the post-repair re-inspection (holon.js:793) re-yields "partial" with nothing new flagged. Not volatile — quotes.js and holon.js are outside the concurrently edited files.

### 2. [high] cite.js:151

namesIn uses an ASCII-only regex (`[A-Z][\w.'-]*`, no /u flag), so accented names are invisible to the names veto — a sentence attributing a phrase to an invented accented subject is attributed with a false address where the identical ASCII sentence is vetoed.

**Evidence:** cite.js:151: `for (const m of s.matchAll(/\b([A-Z][\w.'-]*(?:\s+[A-Z][\w.'-]*)+)\b/g)) names.add(m[1]);` — JS `\w` is [A-Za-z0-9_] and `[A-Z]` excludes É, so "Hélène"/"Éloise" break at the accent ("Anna Pávlovna" extracts as "Anna P"). Verified: namesIn("Éloise Dupré ran a search whose reason was MNPD BOLO.") → ["MNPD BOLO","MNPD","BOLO"] (subject missing entirely); against the Hendersonville rows from cite.test.mjs, attribute() returns ref "searches.csv#14-82" for the Éloise sentence while "Bryan TX PD ran a search whose reason was MNPD BOLO." is refused (ref null, vetoed: true) — the exact live failure documented at cite.js:161-164, resurrected for any non-ASCII subject. namesSupported (line 170) folds diacritics on both sides, but the fold never gets a chance because extraction fails upstream. Contrast grounding.js:267-268 PROPER_RE, which correctly uses `\p{Lu}[\p{L}]*` with /u — the two organs disagree on what a name is, on a repo that reads War and Peace (Hélène, Bezúkhov, Pávlovna).

**Verification:** cite.js:151 contains the quoted ASCII-only regex verbatim (`/\b([A-Z][\w.'-]*(?:\s+[A-Z][\w.'-]*)+)\b/g`, no /u). Executed against the real modules with cite.test.mjs's own Hendersonville fixture: namesIn("Éloise Dupré ran a search whose reason was MNPD BOLO.") returns only ["MNPD BOLO","MNPD","BOLO"] — the invented subject is invisible because É fails [A-Z] and "Dupré" cannot form the required two-capital run — and attribute() then attaches ref "searches.csv#14-82" (score 2, floor 0) to that sentence, while the identical ASCII sentence "Bryan TX PD ran a search whose reason was MNPD BOLO." returns {ref: null, vetoed: true}. Also reproduced "Anna Pávlovna spoke." → ["Anna P"]. No guard rescues it: namesIn's only caller namesSupported (cite.js:171) passes raw unfolded text, so the foldDiacritics at lines 170/175 runs only after extraction has already failed; tokenize folds ("eloise","dupre") but is unused for names. grounding.js:267-268 PROPER_RE does use \p{Lu}[\p{L}]* with /u, confirming the two organs disagree; its atom tier would flag the accented name in the separate grounding report but does not withdraw the inline address attribute() stamps — the names veto is the documented mechanism (cite.js:161-164) for refusing exactly that address. Not volatile: cite.js is not app.js/index.html.

### 3. [high] serve.mjs:260

serve.mjs carries two egress-capable execution paths (/api/exec terminal, /api/run build runner) that no policy sanctions, no record captures, and no test covers — P13 amended P1 to permit exactly ONE egress and it is not this one.

**Evidence:** POLICIES.md P1: "No request leaves the machine ... there is no API-key path and none may be added"; P13: "What is added ... is ONE egress, in the local server only (`explore-server.mjs` `/api/web/*` ...) ... Everything P1 says about the model, the page, and API keys stands unchanged." serve.mjs's own header (lines 23-26): "Python builds and the terminal share ONE project-local virtualenv (`.venv` ...): `pip install` in the terminal lands where every future build's `python` will import from. npm installs land in this repo's own node_modules the same way" — pip/npm installs are network crossings. Line 302 spawns the PTY: `const proc = spawn("python3", [join(ROOT, "tools", "pty-exec.py"), command], {...})`; line 163 runs model-authored build code raw: `const proc = spawn(cmd[0], [...cmd.slice(1), String(params.code ?? "")], { stdio: ["ignore", "pipe", "pipe"] })` — bypassing every wall P14 built for model-authored procedures (empty vm context, declared organ grants, forbidden-token scan). Neither handler writes a record: the file's only record writer is `recordBuild` (line 102), called only inside /api/build-record — against FOLD-CONSTITUTION I.5 ("holding what was run") and against P16, which records even a download as a crossing. Grep of POLICIES.md and FOLD-CONSTITUTION.md finds zero mentions of terminal/exec/venv/pip, so the feature landed (commit a220dce) with no policy and no test — the exact thing P10 forbids ("a new policy without one is marked as a wish until its test exists"). serve.mjs has no test file at all. Failure scenario: the model emits build code containing urllib.request.urlopen(...); the user clicks Run; the request leaves the machine with nothing on any record and every test green.

**Verification:** Every quote verified byte-exact: P1 (POLICIES.md:11-12 "No request leaves the machine...none may be added"), P13 (:214-215 sanctions ONE egress, explore-server.mjs /api/web/* only), the pip/npm header (serve.mjs:23-26), the raw spawn of model-authored code (serve.mjs:163, no vm/token-scan/grants), the PTY spawn (:302), and recordBuild (:102) called only inside /api/build-record — neither /api/run nor /api/exec writes any record server-side. Grep confirms zero mentions of exec/terminal/venv/pip in POLICIES.md, FOLD-CONSTITUTION.md, or even CLAUDE.md. git show a220dce confirms the feature landed with no policy amendment and no test (only editor.js added to the II.13 host-scan list, which cannot see server-side egress); no serve.test.mjs exists and builds.test.mjs:3-4 admits the server half is "exercised by hand". Failure scenario traced end to end: app.js:1944/2298 POST to /api/run → python -B -c <code> with inherited network → urllib egress, all tests green. One overstatement (volatile, app.js): a UI-initiated build run IS mirrored to record/build-record.jsonl (attachRun + mirrorBuild), so "nothing on any record" is wrong in the letter for that one path — but the network crossing is never recorded as a crossing (P13's requirement), a direct POST to /api/run leaves zero record, and the /api/exec terminal (whose stated purpose includes pip install) is recorded nowhere at all. The user-click gate and the "not a security boundary" stance exist only as code comments (serve.mjs:51-52, 62-64), which is exactly what P10 marks as a wish.


> **Status update (same day):** the /api/exec PTY terminal and tools/pty-exec.py were DELETED by the browser-sandbox-terminal session (P18 landed: terminal runs in the browser sandbox only). The /api/exec half of this finding is resolved by removal. **/api/run remains** — loopback-only, but still policy-unsanctioned and server-unrecorded; that residue stands.

### 4. [high] constitution.test.mjs:97

P1's stated enforcement — "constitution.test.mjs fails on any non-localhost host in any file the page loads" — is actually a hardcoded 13-file scan that misses at least ten modules the page loads today, so the claimed invariant is unenforced and already false in the letter.

**Evidence:** POLICIES.md P1: "**Enforced:** `constitution.test.mjs` fails on any non-localhost host in any file the page loads"; CLAUDE.md: "Hard rule enforced by constitution.test.mjs: **no non-localhost host anywhere**". The test's list (line 97) is: `for (const file of ["app.js", "index.html", "holon.js", "constitution.js", "fold.js", "source.js", "cite.js", "grounding.js", "tables.js", "artifact.js", "reflex.js", "editor.js", "build-log.js"])`. app.js currently imports render.js (line 64), provenance.js (68), pace.js (70), cast.js (101), hypergraph.js (113), web.js (128), bound.js (~144), model-routing.js (62), quotes.js (via holon.js line 42), folds-pane.js (46); index.html loads log-pane.js (line 1293) and `/node_modules/monaco-editor/min/vs/loader.js` — none scanned here, by proof.test.mjs (line 151 scans only proof.js/app.js/index.html), or by web.test.mjs (line 234 scans only explore/explore.js, explore.html, explore-bridge.js). Standing counterexample: web.js is page-loaded and names a non-local host at line 253 — `if (/^\/web\/\d+/.test(cl)) return \`https://web.archive.org${cl}\`;` — legitimate data for the server's archive naming, but proof the invariant the policy claims is not the one the test enforces. Failure scenario: `import "https://cdn.example.com/x.js"` added to render.js ships with all 287 tests green while the page fetches remote.

**Verification:** Confirmed byte-for-byte. constitution.test.mjs:97 contains exactly the quoted 13-file hardcoded list, and the file (135 lines, read in full) has no other scan. POLICIES.md:16-17 states verbatim "Enforced: constitution.test.mjs fails on any non-localhost host in any file the page loads"; CLAUDE.md states "no non-localhost host anywhere". Eleven page-loaded modules escape all three host scans in the repo (constitution.test.mjs:97, proof.test.mjs:151 scanning only proof.js/app.js/index.html, web.test.mjs:234 scanning only the three explore files): render.js, provenance.js, pace.js, cast.js, hypergraph.js, web.js, bound.js, model-routing.js, folds-pane.js, quotes.js (imported by holon.js:42), and log-pane.js (index.html:1293) — grep confirms zero mentions of any of them in the three seam tests and no other matchAll(/https scan exists anywhere including explore/, eval/, tools/. The standing counterexample holds: web.js:253 reads `if (/^\/web\/\d+/.test(cl)) return \`https://web.archive.org${cl}\`;`, web.js is page-loaded via app.js's `import { hostOf } from "./web.js"`, and that line would fail the scan's own regex if web.js were listed — so the invariant's letter is already false, not merely unenforced. The failure scenario traces end to end: a remote import added to render.js is caught by no test. Caveats: app.js/index.html line numbers in the finding drift slightly (render.js is line 69 not 64, web.js 133 not 128, log-pane.js confirmed at 1293) — volatile, concurrently edited, but every claimed import exists; and the web.js archive.org string is link-naming data rather than a fetch, exactly as the finding itself characterized it, so the violation is of the policy's letter while the no-page-fetch spirit may still hold.

### 5. [high] holon.js:86

The P4 decomposition gate contains a naked clause-count fast path — `if (clauses.length >= 4) return true;` — that decomposes on length alone, contradicting the file's own header, POLICIES.md P4, and the test's stated rule that anchors, not count, decide.

**Evidence:** holon.js:85-88 reads: `if (clauses.length < MIN_SUBSTANTIVE_CLAUSES) return false;\n  if (clauses.length >= 4) return true;\n  const anchors = clauses.filter(clausePinsAnchor).length;\n  return anchors >= 2;`. The header comment (holon.js:55-58) declares the opposite mechanism: "Clause count alone over-fires on a long single-topic sentence; requiring several clauses to each pin their OWN concrete anchor ... is what separates 'many dependent parts' from 'one ask with many words.'" POLICIES.md P4 (line ~57) declares the gate as "several substantive clauses, each pinning its own concrete anchor (`needsDecomposition`)", and holon.test.mjs:120 names its test "the gate fires on several separately-anchored parts, not on length" — but that test's anchor-less negative case happens to split into exactly 3 clauses, so the >=4 path is never exercised. Failure scenario: "tell me about the mood, how people seemed, whether anyone was unhappy, and what the overall vibe was" splits into 4 substantive clauses with zero anchors and silently triggers holonic decomposition — the mode the project's own eval measured as LOSING to the flat fold (POLICIES P4 evidence: 5/9 vs 7/9). Neither 4 nor the `anchors >= 2` floor (line 88, which also contradicts "each pinning its own") names a giver or a run.

**Verification:** Confirmed byte-for-byte and reproduced live. holon.js:86 contains exactly `if (clauses.length >= 4) return true;` — a count-only fast path that returns before any anchor is counted — while the file's own header (holon.js:55-58: "Clause count alone over-fires... requiring several clauses to each pin their OWN concrete anchor") and POLICIES.md:57-59 ("several substantive clauses, each pinning its own concrete anchor (`needsDecomposition`)") both declare anchors, not count, as the mechanism. Executed the claimed failure input through the real module: "tell me about the mood, how people seemed, whether anyone was unhappy, and what the overall vibe was" splits into 4 substantive clauses with 0 anchors and needsDecomposition returns true, triggering holonic mode — the mode P4's evidence records as losing 5/9 vs 7/9. The test at holon.test.mjs:120 ("the gate fires on several separately-anchored parts, not on length") never exercises the path: I ran its anchor-less negative case through the split regex and it yields exactly 3 substantive clauses, so it takes the `anchors >= 2` branch and line 86 is untested. No guard exists elsewhere — the sole production caller, app.js:752, is `needsDecomposition(question) ? "model" : "flat"` with no downstream anchor check (app.js citation volatile: under concurrent edit; the finding itself is in holon.js, which is not).

### 6. [high] fold.js:37

The entire prompt/record budget cluster in fold.js is naked — nine behavior-steering constants declared in a block with no per-number comment, giver, or run, and none appear in CLAUDE.md's P4 debt list.

**Evidence:** fold.js:37-46 reads verbatim: `export const SUMMARY_MAX_CHARS = 200;\nexport const ENTITIES_MAX = 8;\nexport const CONTEXT_MAX_CHARS = 150;\nexport const FLOW_MAX_CHARS = 200;\nexport const FOLD_MAX_CHARS = 100;\nexport const MAX_FOLDS_IN_PROMPT = 12;\n\nexport const RECORDS_IN_PROMPT = 8;\nexport const RECORD_REFS_MAX = 6;\nexport const RECORD_OPEN_MAX = 4;` — the comment block above (lines 25-35) is System-1/System-2 philosophy and justifies no number. These are not display caps: RECORDS_IN_PROMPT=8 is applied at fold.js:221 `records: [...(prev.records ?? []), record].slice(-RECORDS_IN_PROMPT)` — this rewrites the STORED summary state, so warrant records older than 8 turns are permanently dropped from the working memory the prompt is built from (a trace survives only in the act ledger), silently bounding what CLAUDE.md P1 calls "addressed and re-openable". ENTITIES_MAX, MAX_FOLDS_IN_PROMPT and the four char caps set what the model is ever shown of the past. The CLAUDE.md P4 debt list (lines 52-55) names only ROWS_PER_CHUNK, NULL_SAMPLES, CORPUS_MINIMUM, MAX_FINDINGS.

**Verification:** fold.js:37-46 matches the quoted nine constants byte-for-byte with no per-number comment (the adjacent RECENCY_WINDOW at line 48-49 is the one constant that does get a comment, proving the convention exists and was skipped for these nine). The state-truncation claim verified: fold.js:221 in addWarrantRecord reads `records: [...(prev.records ?? []), record].slice(-RECORDS_IN_PROMPT)` and app.js assigns its return to state.summary (lines 1014/1123/1396, volatile), so records older than 8 are dropped from stored working memory — silently, with no typed entry; only a prose trace survives via logAct("recorded") in the act ledger, as the finding conceded. The prompt-side re-slice at fold.js:254 shows the stored-state truncation was never needed to bound the prompt. Both CLAUDE.md (lines 52-55) and POLICIES.md P9 (lines 113-120) list exactly ROWS_PER_CHUNK, NULL_SAMPLES, CORPUS_MINIMUM, MAX_FINDINGS as open debt — none of the nine appear. The best refutation, P9's named-budget carve-out, fails by its own text: it requires a stated duty and failures on the record when the budget runs out; the nine state no duty, several set what the model is shown (quality-shaping, not runaway backstops), and the line-221 drop puts nothing on record — in tension with CLAUDE.md P1's "its record is addressed and re-openable" (lines 37-39). Only mitigation: fold.js:3-5 names the eochatX port lineage, a module-level giver but not a per-number giver or run.

### 7. [high] explore/explore.js:1840

Explore's HTML preview renders a source file's raw bytes into an iframe srcdoc with sandbox="" and no CSP, so a previewed page's remote subresources (images, stylesheets, webfonts, media) issue live network requests — a P1 local-only breach the app's other render path deliberately walls off.

**Evidence:** explore.js renderHtmlField (lines 1835-1841): `function renderHtmlField(surface) {` ... `const f = el("iframe");` / `f.setAttribute("sandbox", ""); // no scripts, no same-origin — a picture of the page` / `f.srcdoc = state.text;`. The comment claims safety via 'sandbox', but the HTML `sandbox` attribute gates scripts/forms/same-origin, NOT passive subresource loads. No CSP meta is injected here and neither explore.html nor index.html carries a page-level Content-Security-Policy to inherit (grep for content-security-policy / http-equiv finds only a code comment). Contrast the sanctioned render path: artifact.js CSP_META (line 147-148) = `default-src 'none'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; img-src data:; font-src data:; media-src data:;` is prepended by toDocument (line 150-154) and used by app.js's artifact frame (line 2633: `frame.srcdoc = toDocument(...)`) and the editor preview (line 2033). Explore's preview uses neither. FAILURE SCENARIO: the user opens a saved web page from web/pages/ (CLAUDE.md: 'opening one from history runs the full reading pipeline') or any corpus .html whose modality sniffs to 'html' (explore.js line 1557 dispatches modality 'html' → renderHtmlField), and it contains `<img src="https://tracker.example/beacon?doc=secret">` or a `<link rel=stylesheet href="https://cdn...">`/`@font-face`; on render the browser GETs those non-local hosts, leaking that this machine opened that document (and any data in the URL) — exactly the egress P1 forbids for the very surveillance corpora this instrument exists for. No test exercises this path.

**Verification:** Confirmed at explore/explore.js:1835-1840 verbatim: renderHtmlField sets sandbox="" and f.srcdoc = state.text, where state.text is the raw unsanitized bytes (`state.text = await raw.text()`, line 246; "html" is in TEXTUAL, line 211). Dispatch is real: explore-server.mjs:327 sniffs .html/.htm to modality "html" (browse root = ../, line 62), line 1557 routes it to renderHtmlField, and saved web pages reach it via web.js:262 (.html extension) plus the history "html" button (explore.js:1514, openSource(e.rawPath)); Field is the default face (line 236) so rendering fires on open. No guard exists: explore.html has no CSP meta, explore-server.mjs sets no CSP header at any writeHead/setHeader site, and renderHtmlField injects none — so the srcdoc frame inherits no policy, and sandbox="" does not block passive subresource loads (img/link/font/media), a fact the repo itself documents (artifact.js:143 "A CSP...holds whether or not the sandbox grants scripts"; index.html:1187 "toDocument's CSP keeps the page off the network"). The sanctioned path's wall (artifact.js CSP_META lines 147-148, toDocument 150-153) is absent here. No test covers the path, and the seam tests (web.test.mjs:233-237, constitution.test.mjs:96-105) only regex-scan source files for host literals, so content-triggered egress is invisible to them. File is explore/explore.js, outside the concurrently edited app.js/index.html, so not volatile.

### 8. [medium] web.test.mjs:234

The local-only wall as advertised ('constitution.test.mjs fails on any non-localhost host in any file the page loads') is not enforced over every file the pages actually load: the stylesheet, the log pane, and the vendored module are scanned by no seam test, so a webfont/@import/CDN added to any of them would pass all walls.

**Evidence:** web.test.mjs seam (line 233-244) scans only `for (const file of ["explore/explore.js", "explore.html", "explore-bridge.js"])`; constitution.test.mjs II.13 (line 97) scans `["app.js", "index.html", "holon.js", "constitution.js", "fold.js", "source.js", "cite.js", "grounding.js", "tables.js", "artifact.js", "reflex.js", "editor.js", "build-log.js"]`; proof.test.mjs (line 151) scans `["proof.js", "app.js", "index.html"]`. None scan: (1) `explore/explore.css`, which explore.html line 8 loads (`<link rel="stylesheet" href="explore/explore.css" />`) — CLAUDE.md claims 'no webfonts... enforced by constitution.test.mjs', but a webfont most naturally lives in CSS and no test reads any .css; (2) `log-pane.js`, which index.html loads as a module and which itself calls the network — log-pane.js line 24: `const res = await fetch(\`${base}/api/record?tail=200\`)` with base list including a hardcoded `"http://localhost:8812"` (line 22); (3) `explore/vendor/viselect.mjs`, imported by explore.js line 21 (`import SelectionArea from "./vendor/viselect.mjs"`). FAILURE SCENARIO: a future edit adds `@font-face{src:url(https://fonts.gstatic.com/...)}` to explore.css, or changes log-pane's fallback host to a non-local one, or a vendored-module bump pulls in a `fetch`; every existing seam/constitution test still passes green while the page now loads remote — the wall reports compliance it did not check. All three files are clean today, so no active leak; the defect is the missing enforcement the repo claims to have.

**Verification:** Confirmed against the bytes. The repo advertises whole-page enforcement (POLICIES.md:15-16 "Enforced: constitution.test.mjs fails on any non-localhost host in any file the page loads"; CLAUDE.md:172 "no non-localhost host anywhere — no webfonts, no CDN, nothing"), but the three scanners cover fixed allowlists exactly as quoted: web.test.mjs:234 ["explore/explore.js","explore.html","explore-bridge.js"], constitution.test.mjs:97 (13 files), proof.test.mjs:151 ["proof.js","app.js","index.html"]. Grep of all *.test.mjs for explore.css/log-pane/viselect/readdir/glob: zero hits, and package.json:8 runs only top-level tests — so three page-loaded files are scanned by nothing: (1) explore/explore.css, loaded at explore.html:8 (`<link rel="stylesheet" href="explore/explore.css" />`) — no test reads any .css though a webfont naturally lives there; (2) log-pane.js, loaded at index.html:1293 (`<script type="module" src="log-pane.js"></script>`, volatile citation) and network-touching at lines 22-24 (`for (const base of ["", "http://localhost:8812"])` ... `await fetch(\`${base}/api/record?tail=200\`)`); (3) explore/vendor/viselect.mjs, imported at explore/explore.js:21 (`import SelectionArea from "./vendor/viselect.mjs"`) — the seam scan sees only the relative import string, never the vendor file's bytes. Refutation attempts failed: no readdir/glob-based scan exists anywhere; no CSP guards the main pages (the only CSP is the sandboxed editor-preview iframe, index.html:1187), so a webfont in explore.css or a non-local fallback in log-pane.js would actually load remote with all tests green. Extra byte-level proof the vendor file is unscanned: viselect.mjs:1 and :134 contain https://github.com and https://stackoverflow.com comment URLs that would FAIL the seam regex today if the file were in any scan list, yet the suite passes. All three files are clean today (no remote loads), so this is a real enforcement-coverage gap versus the documented claim, not an active leak. Core anchors are non-volatile files; only the index.html:1293 script-tag citation is volatile.

### 9. [high] holon.js:649

The reproduction detector misses near-verbatim transcription with punctuation/typography drift because foldWs folds only diacritics, case, and whitespace — not the curly quotes, dashes, or commas that quotes.js's CHAR_FOLD already treats as measured, forgivable drift.

**Evidence:** holon.js:610 `const foldWs = (s) => foldDiacritics(String(s).toLowerCase()).replace(/\s+/g, " ").trim();` feeds holon.js:649 `return sf.length > 0 && passagesFolded.some((pf) => pf.includes(sf));` — exact substring containment after the weak fold. quotes.js:72-75 `const CHAR_FOLD = { "‘": "'", "’": "'", "“": '"', "”": '"', "–": "-", "—": "-", ... }` proves the codebase already knows this drift class, but foldWs does not reuse it, and foldDiacritics("don’t") !== "don't" (verified). pg2600.txt carries 8,080 curly double-quotes, 6,471 curly apostrophes, 1,891 em dashes. Probe (replicating judge() byte-for-byte from holon.js:609-665): a full transcription of chunk pg2600.txt#15203-15546 behind one lead-in sentence judges {reproduced:true} byte-exact but {reproduced:false} with quotes/dashes straightened; dropping only the commas from a transcription of pg2600.txt#8854-9297 also yields {reproduced:false}. 3,823 of 10,748 chunks have >half their floor-passing sentences carrying curly typography, so a typography-normalizing model (the common decode behavior) defeats the majority cut across a third of the corpus.

**Verification:** Verified against the bytes: holon.js:610 foldWs uses foldDiacritics (source.js:40-43, NFD + strip of combining marks U+0300-U+036F only), which demonstrably leaves curly quotes/apostrophes/em-dashes intact (foldDiacritics("don’t") === "don’t", executed), while quotes.js:72-75 CHAR_FOLD folds exactly those glyphs — so the codebase's own drift class is absent from the reproduction detector's fold. holon.js:649's exact substring containment therefore fails on typography-straightened transcription. No other guard catches it: applyQuotes (holon.js:788-795) runs after the final judge() and re-runs only inspect(); extractQuotedSpans (quotes.js:121) only sees quote-mark-delimited spans; checkGrounding rewards, not flags, transcription; the streaming predictor (holon.js:689) uses the same reproducedFromContent. End-to-end probe with the repo's real splitSentences/tokenize on the-fold/pg2600.txt reproduced the claimed flips: slice 15203-15546 behind a lead-in judges reproduced=true byte-exact (5/7 verbatim) but false with quotes/dashes straightened (3/7); slice 8854-9297 flips true (6/10) to false (2/10) by dropping commas alone. Only caveat: the finding's corpus-wide figures are slightly off from my counts (I measure 9,003/7,261/2,121 curly-dquotes/apostrophes/em-dashes vs their 8,080/6,471/1,891) and the 3,823-of-10,748-chunks claim was not independently re-derived, but the mechanism and both probe results are confirmed exactly.

### 10. [high] holon.js:657

Verbatim reproduction of short-sentence (dialogue-heavy) material is invisible to the majority cut: sentences under MIN_CONTENT_TOKENS can never count as verbatim but still count in the denominator, and any non-framing lead-in sentence defeats the wholeBlockCopied contiguity test.

**Evidence:** holon.js:647 `if (tokenize(sentence).length < MIN_CONTENT_TOKENS) return false;` (floor 3; tokenize drops stopwords and tokens ≤2 chars, so "Well, and what then?" = 1 content token) combined with holon.js:657 `return wholeBlockCopied || verbatimCount > content.length / 2;` — under-floor sentences inflate content.length while verbatimCount stays 0. Probe on the real corpus: chunk pg2600.txt#611017-611179 has 12/13 sentences under the floor; transcribing it verbatim in full behind one lead-in sentence ("The exchange unfolds between the two speakers as follows.") judges {echoed:false, reproduced:false} — only 1/13 sentences is verbatim-countable and the lead-in breaks pf.includes(contentText) contiguity. 1,137 chunks with ≥4 sentences have ≥half their sentences under the floor: the whole dialogue face of War and Peace can be photocopied past the guard.

**Verification:** Verified in the bytes and by executing the repo's own code. holon.js:645-657 reads exactly as cited: MIN_CONTENT_TOKENS = 3; isVerbatimSentence returns false for any sentence with fewer than 3 content tokens (line 647), yet those sentences remain in `content`, so line 657's `verbatimCount > content.length / 2` counts them only in the denominator. source.js:59 confirms tokenize drops stopwords and tokens ≤2 chars; executed, tokenize("Well, and what then?") = ["well"] (1 token). The probe chunk is real: the repo's own chunkSource("pg2600.txt", ...) emits a chunk at exactly #611017-611179 (the "Nikólenka... a letter... wounded..." dialogue) which cite.js:29's splitSentences cuts into 13 sentences, 12 under the floor. A byte-faithful replica of judge() (holon.js:608-665) using the repo's own tokenize/splitSentences/foldDiacritics judges one lead-in sentence + the full verbatim chunk as {echoed:false, reproduced:false}: verbatimCount=1 of 13 content sentences, and the lead-in breaks the pf.includes(contentText) wholeBlockCopied test (line 655). No other guard catches it: the correction loop (755-762) fires only on reproduced/echoed/unsupported and a verbatim copy is fully grounded; refs are stripped only when the verdict fires (825-826); the streaming abort (689) calls the same reproducedFromContent; quotes.js:132 only inspects text inside curly quotation marks and verbatim quotes are cited, not failed. Corpus-wide claim exact: 1,137 of 11,132 chunks have ≥4 sentences with ≥half under the floor, and all 1,137 evade the majority cut behind one lead-in. The probe even showed pure no-lead-in transcription of the cited chunk also evades (a short sentence whose tokens all appear in the question is classed as framing, breaking contiguity). holon.js is not a volatile file (only app.js/index.html are).

## Unverified (reported by finders, not yet adversarially checked)

- **[medium] quotes.js:168** — locateSegment builds quote anchors by adding indices into the TRIMMED chunk.text onto the UNTRIMMED chunk.start, so any chunk whose body had leading whitespace trimmed yields an anchor naming shifted bytes — a coordinate mixup violating P5.2 (an address must read back exactly what it names).
- **[medium] holon.js:751** — When the passage-less chat fallback succeeds, runPart replaces `draft` but not `verdict` or `check`, so the shipped good reply carries a false "answer restates the prompt; nothing established" open entry read off the discarded echoed draft.
- **[low] builds.js:105** — The addendum hash "chain" covers the previous entry's seq number, not its hash, so tampering with an earlier addendum leaves every later hash valid — the "git-like chaining" builds.test.mjs and CLAUDE.md claim does not exist.
- **[medium] constitution.js:48** — The ENFORCEMENT map omits eleven constitution articles entirely (II.2, II.4, II.6, II.7, II.8, II.10, II.12, II.14, III.2, III.6, IV.2) instead of listing them as unwired, defeating the map's own stated purpose that "unwired is VISIBLE" (VI.3).
- **[medium] fold.js:199** — buildWarrantRecord silently slices record lists to their caps, so the plain send() path drops overflow with no visible count — against P6's "never a silent drop" — and no test can catch it because the visible-count discipline lives only in untestable app.js code.
- **[medium] serve.mjs:444** — serve.mjs binds all network interfaces and its static-file branch has no loopback guard, exposing record/*.jsonl, materials/, and web/ (the saved pages and history) to any LAN client — the local-only law has no enforcement at the bind seam.
- **[medium] CLAUDE.md:509** — CLAUDE.md documents a server-owned builds record (POST /api/builds/log writing record/builds/<slug>.jsonl and projecting into materials/) that exists in no server file — a documented enforcement point that is not wired.
- **[low] log-pane.js:47** — log-pane.js, a new untracked page module created today, ships pure functions (summarizeEvent, flattenFields) with no test file, against P10's change-lands-with-its-test discipline.
- **[high] fold.js:49** — RECENCY_WINDOW = 4 has its role canonized in doctrine but the value 4 never names a giver or a run, and it silently propagates into three other behaviors: the surprise meter's window, the derived forgetting rate gamma = 0.75, and the self-plane recency slice.
- **[medium] fold.js:198** — buildWarrantRecord's `clean` helper silently slices record lists to their caps with no overflow count, while POLICIES.md P6 declares the opposite ("never a silent drop") — only the holonic call site adds the visible "+N more" count; the ordinary bound turn and the self turn do not.
- **[medium] source.js:301** — The retrieval count `limit = 3` — the number of passages every turn's entire evidence pipeline (grounding, attribution, citation, hypergraph) runs against — is a naked default; holon.js's PASSAGES_PER_PART even names it as its giver, chaining a declared constant to an undeclared root.
- **[medium] app.js:201** — MAX_TOKENS = 4096, the decode budget of every ordinary turn, is completely uncommented — in a file where the neighboring constant gets a four-line justification and holon.js explicitly calls this same 4096 "a standing permit to transcribe a whole chapter".
- **[medium] source.js:221** — ROW_CHUNK_CHARS = 1200 and SEGMENT_MAX_CHARS = 4000 are chunk-geometry constants that steer address granularity exactly like the debt-listed ROWS_PER_CHUNK, but neither appears in CLAUDE.md's P4 debt list and neither value is justified — the debt ledger under-counts its own category.
- **[medium] holon.js:677** — PREDICT_EVERY_CHARS = 200, the cadence at which a streaming part answer is tested for verbatim-copy domination and possibly cancelled mid-generation, states its duty but not its value — no giver, no run.
- **[low] holon.js:61** — MIN_CLAUSE_WORDS = 3 and MIN_SUBSTANTIVE_CLAUSES = 3 gate the same P4 decomposition decision as the >=4 fast path; the header explains the heuristic's shape but neither 3 names a giver, and they are absent from the debt list.
- **[low] hypergraph.js:194** — An inline token-length floor `if (t.length < 3) continue;` in endpoint() silently decides which words can ever match a relation edge's endpoints, and it is a different, undeclared floor than the MIN_STEM = 4 the same file declares with a named lineage two screens up.
- **[low] eval/dialogue.mjs:78** — The eval harness's decode budgets — ANSWER_MAX_TOKENS = 512, QUESTION_MAX_TOKENS = 100, FOLD_MAX_TOKENS = 300 — are naked, and they shape the long-stream eval that the project treats as its evidence base.
- **[low] builds.js:36** — BUILD_MESSAGE_MAX = 200 is uncommented in a file where the neighboring exports each carry a declaration, and its truncation shows an ellipsis but no count of what was dropped.
- **[low] explore/explore.js:2443** — The Network-terrain ties list is capped by a bare inline 24, duplicated at two sites that must agree by hand; the overflow count is visible (P6-compliant) but the number itself names no giver.
- **[medium] provenance.js:95** — stripScaffoldNarration's >=2-sentence rule lets bracketed self-narration through when it is one run-on sentence, and bracketSpans drops narration entirely when the model never closes the bracket — in both cases the narration ships as content and its tokens block the echoed verdict.
- **[medium] holon.js:634** — The echo verdict fires only when EVERY content token of EVERY sentence is a question/ACT word, so a restated question carrying one filler token per sentence passes as content — and since refs are cleared only on the verdict, attribution can still chip the near-echo, the exact sub-class wake 11 measured that the guard was meant to close.
- **[medium] holon.js:751** — When the passage-less chat retry succeeds, the draft is replaced but neither verdict nor check is recomputed, so the record ships a false typed open ('answer restates the prompt; nothing established') plus the discarded draft's unsupported findings attached to a good reply — a record that misdescribes the text the reader sees.
- **[low] holon.js:611** — The reproduction instrument reads only the offered passages while the quotation tier reads the whole live pool — an answer transcribing a live chunk the part was not offered (from an earlier part's material or model memory of the corpus) is invisible to judge().
- **[low] quotes.js:127** — A fabricated quotation of four words or fewer is never extracted, so it never reaches quoteFindings and ships inside quotation marks unflagged — the strongest claim shape in the design's own terms, exempt below a declared floor.
- **[low] cast.js:127** — Watch-log cross-check: of the failure classes recorded in eval/results/watch-log.md, all are now mechanically guarded except two — the transliteration-variant class (runs 2, 3, 6: Prince Andrew/Andrei, Natalia, 'Napoleonic Wars') has no guard anywhere in the code, only a documented seam awaiting a 'received prior' module that does not exist in the repo; and the near-echo-chipped-by-attribution class is only half-closed (see the isFraming finding).
