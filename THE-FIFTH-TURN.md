# The Fifth Turn — level 5 walked through all 27 phaseposts

*The corroboration floor read cell by cell, 2026-09-01. A "phasepost" is
the engine's own word (eoreader7 `adapters/text/phasepost.js`): one of
the 27 operator × grain cells, as an act of transformation. Level 5's
operands are NOTES — assertions that have become variables over their own
restatements — and this document asks, for every cell, what that act IS
at note-grade, which organ performs it, and what is honestly missing.
Standing: a working map beside NEXT-PASSES.md; where it disagrees with
the code, the code and its tests win.*

The grain columns follow THE-THREE-MATHEMATICS §III: Ground = the
reference, Figure = the first difference from it, Pattern = the
difference that moves the reference.

## The map

| op | Ground | Figure | Pattern |
|---|---|---|---|
| **NUL** | the ledger's void — "no note on this subject." Exists as behavior (an empty fold), **named-not-built as a TYPED answer** — a queryable "no standing" for the chat's use. | `attest`'s `unknown_note` refusal — this particular note does not exist. **Built.** | the standings report's zero-counts — a KIND of note absent from a run. **Built** (zeros are reported, never implied). |
| **SIG** | the witness string — WHO vouches, namespaced (`testimony:` vs mechanical) so kinds of vouching are never confused. **Built.** | a span att