// the-fold/cervantes.js — the ethos-corpus role carrier for the archon named in
// eo-teachings/manifest/cervantes.json.
//
// Handle: Cervantes — the windmill — the ideal meeting the real; the felt gap between the claimed and the undergone, held as comedy, never as judgment
//
// This file is a role-carrier, not an organ: it casts the deep role the
// manifest names, and pythia reads the role from this header when the
// archon speaks.
