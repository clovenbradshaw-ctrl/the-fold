// the-fold/pascal.js — the ethos-corpus role carrier for the archon named in
// eo-teachings/manifest/pascal.json.
//
// Handle: Pascal — the wager — the point where reason must stake on the heart; the reasons that reason does not know
//
// This file is a role-carrier, not an organ: it casts the deep role the
// manifest names, and pythia reads the role from this header when the
// archon speaks.
