// the-fold/sequence.js — a re-export. The sequence type was admitted to the
// kernel by measurement (eval/sequence-admission.mjs: retrieval, reasoning
// and prediction all demonstrably improved over the shipped apparatus, with
// the pre-registered prediction arm's FAILURE kept in the record as the
// finding that produced `refuteLocus`). The organ lives with the engine;
// this file keeps the proving-ground import path alive.
export { ALGEBRA, declareSequence, readSequence, refuteLocus, predictNeighbour, locusOf, sameLocus } from "../eoreader7/native/kernel/sequence.js";
